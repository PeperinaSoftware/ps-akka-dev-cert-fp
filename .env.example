# Copy to .env in the repo root (never commit .env).
# See README → API keys.

GOOGLE_AI_GEMINI_API_KEY=
OPENWEATHERMAP_API_KEY=

# Optional overrides:
# OPENWEATHER_LOCATION_QUERY=Cordoba,AR
# ALLOW_SIMULATED_QUOTA_FALLBACK=true
# RESERVATION_EVALUATION_DEADLINE=90s
